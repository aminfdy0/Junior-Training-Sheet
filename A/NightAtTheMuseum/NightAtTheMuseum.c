#include <stdio.h>
int main(void)
{
    char name[100];
    scanf("%s", &name);

    //  z     e     u     s
    // 122   101   117   115
    int count = 0;
    for (int i = 0; name[i] != '\0'; i++)
    {
        if(name[i+1] >= 110)
        {

        }
        else if (name[i+1] < 110)
        {
            
        }
        
    }
    
}